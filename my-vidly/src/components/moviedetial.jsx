import React from 'react';
const MovieDetial = ({ id }) => {
    return (
        <React.Fragment>
            <p>Movie From {id}</p>
            <button>save</button>
        </React.Fragment>);
}

export default MovieDetial;